from mcp.server.fastmcp import FastMCP

app = FastMCP()

@app.tool()
def add(a: int, b: int) -> int:
    """
    Add two numbers
    """
    return a + b

if __name__ == "__main__":
    print("Starting MCP server on streamable-http...")
    app.run(transport="streamable-http")
"""
Што всушност прави MCP

МCP дефинира:

како се опишуваат алатки (што прават, кои параметри имаат)

како се повикуваат

како се враќа резултатот

како моделот дознава што е дозволено

➡️ Ништо повеќе, ништо помалку.

Агент е LLM што:

има цел

сам одлучува што да направи

користи алатки

го проверува резултатот

повторува чекори ако треба

👉 Обичен LLM само одговара.
👉 Агент делува.


Зошто сите зборуваат за агенти

Затоа што:

сакаме AI што работи, не само зборува

сакаме автономија, не копи-пејст

сакаме системи што извршуваат задачи

Агенти се мостот меѓу:

LLM

реални системи

| LLM           | Агент         |
| ------------- | ------------- |
| одговара      | делува        |
| пасивен       | активен       |
| 1 чекор       | повеќе чекори |
| нема состојба | има состојба  |


"""