import asyncio
from langchain_mcp_adapters.client import MultiServerMCPClient

async def main():
    client = MultiServerMCPClient(
        {
            "math": {
                "url": "http://127.0.0.1:8000/mcp",
                "transport": "streamable_http",
            }
        }
    )

    tools = await client.get_tools()
    print("Available tools:", [t.name for t in tools])

    add_tool = next(t for t in tools if t.name == "add")

    result = await add_tool.arun({"a": 12, "b": 30})
    print("Add result:", result)

if __name__ == "__main__":
    asyncio.run(main())
"""
Овој код:

Се поврзува со MCP сервер

Ги чита достапните алатки

Ја избира алатката add

Ја повикува со броеви 12 и 30

Го печати резултатот

8️⃣ Зошто ова е моќно

Со MCP можеш:

да повикуваш база на податоци

да читаш фајлови

да правиш пресметки

да интегрираш реални системи



1️⃣ Што е MCP (Model Context Protocol)

MCP е стандарден начин:

LLM / агент (како LangChain)

да комуницира со надворешни функции

преку сервер (HTTP, stdio, итн.)

👉 Наместо моделот „сам да измислува“, тој:

прашува сервер: „Кои алатки ги имаш?“

повикува конкретна алатка со параметри

добива реален резултат

📌 Во твојот пример:

MCP серверот нуди алатка add

клиентот ја повикува таа алатка



"""